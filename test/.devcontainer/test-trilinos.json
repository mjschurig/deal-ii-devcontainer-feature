{
  "name": "Test deal.II with Trilinos",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  "features": {
    "../src/deal-ii": {
      "version": "9.5.0",
      "enableMPI": true,
      "enableTrilinos": true,
      "buildThreads": "2"
    }
  },
  "postCreateCommand": "cd /workspaces/*/test && mkdir -p build && cd build && cmake .. -DDEAL_II_DIR=${DEAL_II_DIR} && make && ./test_app",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools"
      ]
    }
  }
} 